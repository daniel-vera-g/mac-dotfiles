# Neovim
alias v="nvim"
alias lv="lvim"

# Git
alias g="git"
alias gs="git status"
alias gam="git add . && git commit -m"
alias gc="git checkout"
alias gcb="git checkout -b"

# General
alias x+="chmod +x"
alias cppt="pwd | pbcopy"
alias refresh="source ~/.zshrc"

# cli apps
alias lzg="lazygit"
alias lx="exa --long --header"
alias codenx="code --disable-extensions"
alias cat="bat"

# Tmux
alias t="tmux"
alias ta="t a -t"
alias tls="t ls"
alias tn="t new -t"
alias tmuxKillS="tmux kill-session -t"
alias tmuxRenS="tmux rename-session -t"
alias tmuxNewSess="tmux new-session -s"
alias tmuxNewSessN="tmux new-session -t"

# Docker
alias dk="docker"
alias dkps="docker ps"
alias dkpsa="docker ps -a"
# Compose
alias c="docker-compose"

# Utils
alias simpleWebServer="python3  -m http.server 8080 --bind 127.0.0.1"
alias ten='trans en:de'
alias tde='trans de:en'

# TODO
# alias caff60='caffeinate -d -t  $(expr 60 \* 60)'
# alias ejectAll="osascript -e 'tell application 'Finder' to eject (every disk whose ejectable is true and local volume is true and free space is not equal to 0)'"
